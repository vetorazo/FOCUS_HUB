<div class="container my-3">
  <h1>All lenses you can find 👇</h1>
    <% @lenses.each do |lens| %>
      <hr>
        <h5><strong><%= lens.brand %></strong></h5>
        <p>Model: <%= lens.model %></p>
        <p>Description</p>
        <ul>
          <% lens.description.each do |description_item| %>
            <li><%= description_item %></li>
          <% end %>
        </ul>
    <% end %>
</div>
